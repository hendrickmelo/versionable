.*
docs/_build
docs/plans
node_modules
.pixi

# Not Markdown: the Roslyn release tracker parses these itself and rejects reflowed tables (RS2007).
dotnet/src/Versionable.Analyzers/AnalyzerReleases.*.md
