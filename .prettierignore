.*
# AGENTS.md carries a block pasted verbatim by an external sync script and
# compared byte-for-byte; reflowing it makes the two copies diverge forever.
AGENTS.md
docs/_build
docs/plans
node_modules
.pixi
